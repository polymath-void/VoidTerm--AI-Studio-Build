<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0A0A0A">

    <!-- The Hardware Accelerated Output Surface -->
    <com.hybridengine.terminal.TerminalSurfaceView
        android:id="@+id/terminal_surface"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/input_container" />

    <!-- The Settings Gear FAB Floating on Top -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_settings"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="16dp"
        android:contentDescription="Settings"
        app:fabSize="mini"
        app:srcCompat="@android:drawable/ic_menu_preferences"
        app:backgroundTint="#222222"
        app:tint="#E0E0E0" />

    <!-- The Minimalist Input Control Row -->
    <LinearLayout
        android:id="@+id/input_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#121212"
        android:orientation="horizontal"
        android:padding="8dp">

        <EditText
            android:id="@+id/command_input"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@android:color/transparent"
            android:fontFamily="monospace"
            android:hint="user@hybrid-engine:~$"
            android:imeOptions="actionSend"
            android:inputType="textNoSuggestions|textVisiblePassword"
            android:textColor="#E0E0E0"
            android:textColorHint="#555555"
            android:textSize="16sp" />

        <Button
            android:id="@+id/btn_send"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="#E0E0E0"
            android:fontFamily="monospace"
            android:text="EXE"
            android:textColor="#0A0A0A"
            android:textStyle="bold" />
    </LinearLayout>
</RelativeLayout>
